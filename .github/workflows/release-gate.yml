name: Release gate

# Runs only on PRs targeting main (develop → main release PRs and hotfixes).
# Not triggered on develop PRs — keeps day-to-day CI fast.
on:
  pull_request:
    branches: [main]

concurrency:
  group: release-gate-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  integration-tests:
    name: Integration tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-python-uv
      # No tests are currently decorated with @pytest.mark.integration.
      # Running the full non-GUI suite here until integration tests are tagged.
      # TODO: apply @pytest.mark.integration to slow real-data tests and switch
      # this back to: uv run pytest -m "integration and not gui" -v
      - run: uv run pytest -m "not gui" -v

  full-matrix:
    name: Tests (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
      fail-fast: false
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-python-uv
        with:
          python-version: ${{ matrix.python-version }}
      - run: uv run pytest -m "not gui"

  docs-build-check:
    name: Docs build verification
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-python-uv
      # -W treats warnings as errors — catches broken refs / missing TOC entries
      # before they reach main.
      - run: uv run sphinx-build -W -b html docs _build

  version-bump-check:
    name: Version bump check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Verify pyproject.toml version changed vs base
        run: |
          BASE_SHA="${{ github.event.pull_request.base.sha }}"
          BASE_VERSION=$(git show "$BASE_SHA:pyproject.toml" | grep -E '^version\s*=' | head -1 || echo "")
          HEAD_VERSION=$(grep -E '^version\s*=' pyproject.toml | head -1)
          echo "Base version line: $BASE_VERSION"
          echo "Head version line: $HEAD_VERSION"
          if [ "$BASE_VERSION" = "$HEAD_VERSION" ]; then
            echo "::error::pyproject.toml version was not bumped vs base. Update it before merging to main."
            exit 1
          fi
          echo "Version bump detected."

  release-gate-success:
    name: Release gate success
    needs: [integration-tests, full-matrix, docs-build-check, version-bump-check]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Check all release gate jobs passed
        run: |
          if [[ "${{ contains(needs.*.result, 'failure') }}" == "true" || "${{ contains(needs.*.result, 'cancelled') }}" == "true" ]]; then
            echo "One or more release gate jobs failed or were cancelled."
            exit 1
          fi
          echo "All release gate jobs passed."
