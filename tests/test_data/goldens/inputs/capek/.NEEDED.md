# capek — needed for BlankCorrector + FinalFileCreation tests
Status: **needs human curation** ⏳
This fixture supplies the "project-style" tree that `BlankCorrector` walks via
`Path.iterdir()`. Subfolder names must keep the date pattern (`MM.DD.YY`) and
contain the substring `blank` for blank folders.
The synthetic tests (most of Phase 2.c) DO NOT depend on this fixture — they
build their own tmp_path projects via `synthetic_blank_folder`. Curating this
folder unlocks 4 real-data tests currently skipped:
| Test | What it needs |
|---|---|
| `test_blank_correction.py::TestFindBlankFiles::test_finds_blank_folders` | >= 1 `* blank/` subfolder with an `INPs_L_*.csv` inside |
| `test_blank_correction.py::TestFindBlankFiles::test_finds_files_from_each_blank_subfolder` | >= 2 `* blank/` subfolders, each with an `INPs_L_*.csv` |
| `test_blank_correction.py::TestAverageBlanks::test_capek_combined_blank_golden` | same as above; result regen via `OLAF_REGEN_GOLDEN=1` into `goldens/expected/test_blank_correction/capek_combined_blank.csv` |
| `test_blank_correction.py::TestApplyBlanks::test_capek_blank_corrected_golden` | additionally needs >= 1 NON-blank sample folder with an `INPs_L_*.csv` (real `blank_corrected_*.csv` useful as reference) |
## Files to copy from real data
Source folder: `tests/test_data/capek/` (not committed).
### Sample / blank subfolders (REQUIRED — keep folder names INTACT)
| Subfolder to create here | From | Files to keep |
|---|---|---|
| `KCG 05.21.24 07.19.24 blank/` | `tests/test_data/capek/KCG 05.21.24 07.19.24 blank/` | `INPs_L_frozen_at_temp_reviewed_capek 05.21.24 blank.csv` and `INPs_L_frozen_at_temp_reviewed_capek 07.19.24 blank.csv` |
| `KCG 07.10.24 08.06.24 blank/` | `tests/test_data/capek/KCG 07.10.24 08.06.24 blank/` | `INPs_L_frozen_at_temp_reviewed_capek 07.10.24 blank.csv` and `INPs_L_frozen_at_temp_reviewed_capek 08.06.24 blank.csv` |
| `KCG 7.09.24 base/` | `tests/test_data/capek/KCG 7.09.24 base/` | latest unnumbered `INPs_L_frozen_at_temp_reviewed_capek 7.09.24 a base.csv` (AND optionally the latest `blank_corrected_10%_error_threshold_INPs_L_*.csv` as reference for the apply_blanks golden) |
| `KCG 7.09.24 heat/` | same site, heat treatment | same pattern (needed for Phase 2.d FinalFileCreation) |
| `KCG 7.09.24 peroxide/` | same site, peroxide treatment | same pattern (needed for Phase 2.d) |
### Final files (Phase 2.d only)
| Path | Purpose |
|---|---|
| `final_files/SGP_C1_*.csv` (all 3) | Golden for `FinalFileCreation.create_all_final_files` |
## Notes
- Use **side A** dilution dict for capek (KCG site).
- Once files are in place, run
  `OLAF_REGEN_GOLDEN=1 pytest tests/test_processing/test_blank_correction.py`
  to populate `goldens/expected/test_blank_correction/*.csv`. Inspect with
  `git diff`, then commit.
- Delete this `.NEEDED.md` manually once curation is done (agent cannot delete).
