# SGP 3.28.24 base — needed for secondary SpacedTempCSV golden

Status: **needs human curation** ⏳

## Files to create from real data

Source folder: `tests/test_data/SGP 3.28.24 base/` (not committed).

Copy the following representative files into this folder with the listed clean
names (same pattern as `sgp_2_21_24_base/`):

| Clean name (place here) | Source filename | Purpose |
|---|---|---|
| `reviewed.dat` | the canonical `reviewed_*.dat` in the real folder (any version is fine — pick the highest `(N)` or the unversioned one) | Input for `SpacedTempCSV` |
| `frozen_at_temp_expected.csv` | the canonical / latest `frozen_at_temp_reviewed_*.csv` (the file that represents your manually-verified expected output) | Golden output for `SpacedTempCSV` on this dataset |

## Test that will use these

- `tests/test_processing/test_spaced_temp_csv.py::TestCreateTempCSV::test_air_sample_sgp_3_28_golden`
  *(currently not in the suite — will be added in Phase 2.b when this folder is ready)*

## Notes

- Use **side A** dilution dict (same as SGP 2.21.24): `{Sample_0:1, ..., Sample_5:inf}`.
- Same `num_samples=6`, `wells_per_sample=32`, `sample_type="air"`.
- Delete `.NEEDED.md` once files are in place (or replace with a `README.md` like SGP 2.21).

## Curation log

Curated on 2026-05-26 by copying existing local test data into this fixture folder.
The `.NEEDED.md` file was intentionally left in place for human review/deletion.

| Destination fixture | Source file used |
|---|---|
| `reviewed.dat` | `tests/test_data/SGP 3.28.24 base/reviewed_sgp ment 03.28.24 a base(1).dat` |
| `frozen_at_temp_expected.csv` | `tests/test_data/SGP 3.28.24 base/frozen_at_temp_reviewed_sgp ment 03.28.24 a base(33).csv` |
